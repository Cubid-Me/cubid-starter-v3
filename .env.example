# Browser-safe OIDC and hosted-flow config.
# These values are bundled into the browser because they use NEXT_PUBLIC_*.
NEXT_PUBLIC_CUBID_ISSUER_URL=https://id.cubid.me
NEXT_PUBLIC_CUBID_OIDC_CLIENT_ID=
NEXT_PUBLIC_CUBID_REDIRECT_URI=http://localhost:3000/auth/callback
NEXT_PUBLIC_CUBID_PASSPORT_BASE_URL=https://passport.cubid.me
NEXT_PUBLIC_CUBID_CLEARPASS_PAGE_ID=

# Server-mediated SIWC/OIDC demo config.
# These keep the demo authorization callback and session handling on API routes.
CUBID_SIWC_ISSUER_URL=https://id.cubid.me
CUBID_SIWC_CLIENT_ID=
CUBID_SIWC_REDIRECT_URI=http://localhost:3000/api/cubid/siwc/callback
CUBID_SIWC_SCOPE="openid profile email"

# Server-only Cubid dapp API config.
# Never prefix these with NEXT_PUBLIC_.
# Get the dapp API key from https://admin.cubid.me/.
CUBID_API_BASE_URL=https://passport.cubid.me
CUBID_API_KEY=
CUBID_DAPP_ID=
